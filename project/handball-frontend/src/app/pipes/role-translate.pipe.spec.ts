import { RoleTranslatePipe } from './role-translate.pipe';

describe('RoleTranslatePipe', () => {
  it('create an instance', () => {
    const pipe = new RoleTranslatePipe();
    expect(pipe).toBeTruthy();
  });
});
